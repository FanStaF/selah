<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- A plain Material3 base for the host Activity; the actual look is driven by Compose
         (ui/theme). The overlay is drawn via WindowManager and does not use this theme. -->
    <style name="Theme.Selah" parent="android:Theme.Material.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowBackground">@android:color/white</item>
    </style>
</resources>
